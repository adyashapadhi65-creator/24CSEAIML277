# Q4. Write a program for statistical operations on numpy array
import numpy as np

arr = np.array([1,2,3,4])

print("Max:", np.max(arr))
print("Min:", np.min(arr))
print("Sum:", np.sum(arr))
print("Mean:", np.mean(arr))